%h1 Usuario

%p
  %b Usuario:
  = @usuario.nombre_usuario
%p
  %b Email:
  = @usuario.email

.form-actions
  = link_to 'Volver', usuarios_path, class: 'btn'
  = link_to 'Editar', edit_usuario_path(@usuario), class: 'btn'
  = link_to 'Eliminar', usuario_path(@usuario), :method => 'delete', :confirm => '¿Está seguro?', :class => 'btn btn-danger' 