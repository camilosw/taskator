= simple_form_for @cliente, html: { class: 'form-horizontal' } do |f|
  = f.error_notification
  .form-inputs
    = f.input :nombre
    = f.input :activo
  .form-actions
    = f.submit 'Guardar', class: 'btn btn-primary'
    = link_to 'Cancelar', clientes_path, class: 'btn'
